import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const inter = Inter({ subsets: ["latin"], variable: "--font-sans" });
const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-mono",
});

export const metadata: Metadata = {
  title: "ZASEON — AI-Powered Smart Contract Security Scanner",
  description:
    "Enterprise-grade smart contract vulnerability detection with zero false positives. AI-powered Solidity analysis, automated PoC verification, and professional audit reports.",
  keywords: [
    "security scanner",
    "smart contract audit",
    "vulnerability detection",
    "AI security",
  ],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${inter.variable} ${jetbrainsMono.variable} font-sans antialiased`}
      >
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
